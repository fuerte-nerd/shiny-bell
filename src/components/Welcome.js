import React from "react";
import {
  Dialog,
  DialogTitle,
  Typography,
  DialogContent,
  DialogActions,
  Button,
} from "@material-ui/core";

import { GitHub } from "@material-ui/icons";

const Welcome = () => {
  return <Dialog open={true} fullScreen></Dialog>;
};

export default Welcome;
